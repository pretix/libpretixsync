selectById:
SELECT *
FROM Quota
WHERE id = ?;

selectForItem:
SELECT *
FROM Quota
WHERE id IN (
    SELECT QuotaId
    FROM Quota_Item
    INNER JOIN Item ON Quota_Item.ItemId = Item.id
    WHERE Item.id = :item_id
);
