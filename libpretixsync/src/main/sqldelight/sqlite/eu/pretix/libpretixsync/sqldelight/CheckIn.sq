import java.util.Date;

CREATE TABLE CheckIn (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    datetime TEXT AS Date,
    json_data TEXT,
    listId INTEGER,
    position INTEGER,
    server_id INTEGER,
    type TEXT,
    FOREIGN KEY (position) REFERENCES OrderPosition (id) ON DELETE CASCADE
);
