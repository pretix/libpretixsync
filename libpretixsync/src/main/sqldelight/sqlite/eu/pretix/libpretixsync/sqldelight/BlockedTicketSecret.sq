import kotlin.Boolean;

CREATE TABLE BlockedTicketSecret (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    blocked INTEGER AS Boolean NOT NULL,
    event_slug TEXT,
    json_data TEXT,
    secret TEXT,
    server_id INTEGER,
    updated TEXT
);
