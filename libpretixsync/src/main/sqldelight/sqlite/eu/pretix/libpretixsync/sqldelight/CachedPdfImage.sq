CREATE TABLE CachedPdfImage (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    etag TEXT,
    key TEXT,
    orderposition_id INTEGER
);

CREATE INDEX CachedPdfImage_etag_index ON CachedPdfImage (etag);
CREATE INDEX CachedPdfImage_key_index ON CachedPdfImage (key);
CREATE INDEX CachedPdfImage_orderposition_id_index ON CachedPdfImage (orderposition_id);
