CREATE TABLE ReusableMedium_OrderPosition (
    ReusableMediumId INTEGER REFERENCES ReusableMedium (id) ON DELETE CASCADE,
    OrderPositionId INTEGER REFERENCES OrderPosition (id) ON DELETE CASCADE,
    PRIMARY KEY (ReusableMediumId, OrderPositionId)
);