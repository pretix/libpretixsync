CREATE TABLE TaxRule (
    id INTEGER PRIMARY KEY AUTOINCREMENT ,
    event_slug TEXT,
    json_data TEXT,
    server_id INTEGER
);
CREATE INDEX IF NOT EXISTS TaxRule_server_id ON TaxRule (server_id);
