language: java
jdk:
  - oraclejdk8
  - oraclejdk9
  - openjdk7
sudo: false
script:
    - cd libpretixsync
    - TERM=dumb ./gradlew clean check test build jar
before_cache:
  - rm -f  $HOME/.gradle/caches/modules-2/modules-2.lock
  - rm -fr $HOME/.gradle/caches/*/plugin-resolution/
cache:
  directories:
    - $HOME/.gradle/caches/
    - $HOME/.gradle/wrapper/

